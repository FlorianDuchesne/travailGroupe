{% extends 'base.html.twig' %}

{% block title %}Page d'accueil
{% endblock %}

{% block body %}
	{# /* <style>
				.example-wrapper {
					margin: 1em auto;
					max-width: 800px;
					width: 95%;
					font: 18px / 1.5 sans-serif;
				}
				.example-wrapper code {
					background: #F5F5F5;
					padding: 2px 6px;
				}
			</style> */ #}
	<div class="container">

		<h1 class="text-info m-5 text-center">Prochaines sessions:</h1>

		<div class="card-columns">
			{% for session in sessions %}
				<div class="card p-3">
					<div class="card-body">
						<a href="{{ path('session_show', {id: session.id}) }}">
							<h4 class="card-title text-info">{{ session }}</h4>
						</a>
						<p class="card-text">
							<ul>
								<li>nombre de places :
									{{ session.nbPlaces }}
								</li>
							</ul>
						</p>
					</div>
				</div>
			{% endfor %}
		</div>
	</div>
{% endblock %}
