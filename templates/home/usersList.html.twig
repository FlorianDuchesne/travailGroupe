{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!
{% endblock %}

{% block body %}
	<div
		class="container">


		{# <h1 class="text-center m-3">Profil</h1>
				
						<ul class="table table-hover">
							<li scope="col">Pseudo :</li>
							<li scope="col">Email :</li>
							<li scope="col">Roles :
								<br></li>
							<li class="text-body" href="{{ path('userShow', {id: app.user.id}) }}">{{ app.user.pseudo }}</li>
				
							{{ app.user.email }}
							{{ app.user.listeRoles | raw }}
						 #}

		<h1 class="text-center m-3">Liste des utilisateurs</h1>
		<table class="table table-hover">
			<thead>
				<tr>
					<th scope="col">Pseudo</th>
					<th scope="col">Email</th>
					<th scope="col">Roles</th>
				</tr>
			</thead>
			<tbody>
				{% for user in users %}
					<tr>
						<td>
							{# {{ user.pseudo }} #}
							<a class="text-body" href="{{ path('userShow', {id: user.id}) }}">{{ user.pseudo }}</a>

						</td>
						<td>
							<a class="text-body" href="{{ path('userShow', {id: user.id}) }}">{{ user.email }}</a>

						</td>
						<td>
							{{ user.listeRoles | raw }}

						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
		<a href="{{ path('home') }}">Retour</a>
	</div>
{% endblock %}
