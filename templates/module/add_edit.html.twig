{% extends 'base.html.twig' %}

{% block title %}Ajout module
{% endblock %}

{% block body %}
	{% if editMode %}
		<h1 class="text-center m-3">Modification d'un module</h1>
	{% else %}
		<h1 class="text-center m-3">Ajout d'un module</h1>
	{% endif %}
	<div class="m-auto col-3">
		{{ form(formAddModule) }}
				<button class="btn btn-info">
			<a class="text-white text-center" href="{{ path('module') }}"><i class="fas fa-undo"></i></a>
		</button>

		</div>
	{% endblock %}
